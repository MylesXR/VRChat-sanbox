
using UdonSharp;
using UnityEngine;
using VRC.SDKBase;
using VRC.Udon;

public class PotionEffectsManager : UdonSharpBehaviour
{
    /*
    private bool isPickedUp = false;

    public override void OnPickup()
    {
        isPickedUp = true;
        Debug.Log("Object has been picked up");
    }

    public override void OnDrop()
    {
        isPickedUp = false;
        Debug.Log("Object has been dropped");
    }

    void OnCollisionEnter(Collision collision)
    {
        if (isPickedUp && collision.gameObject.layer == LayerMask.NameToLayer("Ground"))
        {
            Debug.Log("Object has collided with the ground after being picked up");
            // Additional logic for what happens on collision can be added here
        }
    }
    */
}
